# 💬 Quote Generator

Современное и минималистичное приложение для отображения вдохновляющих цитат.
Поддерживает случайный выбор цитат, чистый дизайн и простой интерфейс.

Проект состоит из:

**HTML** — структура приложения

**CSS** — минималистичный визуальный дизайн

**JavaScript** — логика загрузки цитат, случайный выбор

**JSON** — база данных цитат

## 🚀 Возможности

### ✅ База данных цитат

Коллекция из 26+ вдохновляющих цитат от известных личностей:

- **Steve Jobs** — о работе и инновациях
- **Albert Einstein** — о науке и жизни
- **Muhammad Ali** — о силе духа
- **Jason Statham** — мотивационные цитаты
- **Ali ibn Abi Talib** — мудрость
- И многие другие...

### ✅ Случайный выбор цитаты

При каждом нажатии кнопки:

- выбирается случайная цитата из массива
- отображается текст цитаты
- показывается автор

### ✅ Автозагрузка при старте

Приложение автоматически:

- загружает случайную цитату при открытии страницы
- готово к использованию сразу

### ✅ Чистый и минималистичный UI

Приложение включает:

- светлый фон (#f9fafb)
- белую карточку с тенью
- центрированный контент
- приятную типографику
- синюю кнопку с hover-эффектом
- адаптивный дизайн

## 📂 Структура проекта

```
/
│── index.html       # Макет и структура приложения
│── style.css        # Стили: UI, карточка, кнопка
│── main.js          # Логика приложения, загрузка цитат
│── quotes.json      # База данных цитат
│── README.md        # Документация проекта
```

## 📦 Установка

Просто скачай файлы и открой:

```
index.html
```

Никаких зависимостей и сборки не требуется.

## 📝 Формат данных цитат

Каждая цитата в `quotes.json` имеет формат:

```json
{
  "text": "Текст цитаты",
  "author": "Имя автора"
}
```

### Пример:

```json
{
  "text": "The only way to do great work is to love what you do.",
  "author": "Steve Jobs"
}
```

## 🎯 Список цитат

В приложении доступны цитаты от:

| Автор | Количество цитат |
|-------|------------------|
| Steve Jobs | 3 |
| Theodore Roosevelt | 2 |
| Jason Statham | 2 |
| Winston Churchill | 1 |
| Muhammad Ali | 1 |
| Albert Einstein | 1 |
| Ali ibn Abi Talib | 1 |
| Omarov Zhanbolot | 1 |
| И другие... | 14+ |

**Всего:** 26 цитат

## 🔧 Как работает приложение

1. При загрузке страницы вызывается `getNewQuote()`
2. Функция выполняет `fetch()` запрос к `quotes.json`
3. Парсит JSON и получает массив цитат
4. Выбирает случайный индекс: `Math.floor(Math.random() * quotes.length)`
5. Обновляет DOM элементы с текстом цитаты и автором
6. При нажатии на кнопку "New Quote" процесс повторяется

## 🧠 Основные функции

**getNewQuote()** — загрузка и отображение случайной цитаты

```javascript
async function getNewQuote() {
    // 1. Загрузка данных
    const response = await fetch(API_URL);
    const quotes = await response.json();
    
    // 2. Случайный выбор
    const randomIndex = Math.floor(Math.random() * quotes.length);
    const quote = quotes[randomIndex];
    
    // 3. Обновление UI
    quoteText.textContent = `"${quote.text}"`;
    quoteAuthor.textContent = `— ${quote.author}`;
}
```

**Error Handling** — обработка ошибок при загрузке

```javascript
try {
    // fetch логика
} catch (error) {
    console.error('Could not fetch new quote:', error);
    quoteText.textContent = "Error loading quote. Please try again.";
}
```

## 📱 Адаптивный дизайн

Приложение полностью адаптивно и отлично работает на:

- 📱 **Мобильных устройствах** (до 640px)
- 📱 **Планшетах** (640px+)
- 💻 **Desktop** (все размеры)

### Адаптивные изменения:

```css
/* Мобильные */
- Компактные отступы (1rem)
- Меньший заголовок (1.5rem)
- Карточка 180px высотой

/* Desktop (640px+) */
- Увеличенные отступы (1.5rem)
- Больший заголовок (1.875rem)
- Карточка 200px высотой
- Увеличенный размер текста
```

## 🛠️ Используемые технологии

| Технология | Назначение |
|------------|-----------|
| HTML5 | Разметка |
| CSS3 | Минималистичный дизайн, адаптивность |
| JavaScript (ES6) | Асинхронные запросы, случайный выбор |
| JSON | Хранение данных цитат |
| Fetch API | Загрузка данных |

## 🎨 Цветовая палитра

```css
/* Фоны */
Основной фон: #f9fafb (светло-серый)
Карточка: white (#ffffff)

/* Текст */
Заголовок: #1f2937 (тёмно-серый)
Цитата: #374151 (серый)
Автор: #6b7280 (светло-серый)

/* Кнопка */
Основной: #3b82f6 (синий)
Hover: #2563eb (тёмно-синий)
Active: #1d4ed8 (ещё темнее)
```

## 🎯 Особенности дизайна

### Карточка цитаты

```css
- background: white
- border-radius: 1rem (1.5rem на desktop)
- box-shadow: мягкая тень
- min-height: 180px (200px на desktop)
- padding: 1.5rem (2rem на desktop)
```

### Кнопка

```css
- background: синий градиент
- hover: плавное затемнение
- active: ещё темнее
- transition: 0.2s
- cursor: pointer
```

### Типографика

```css
- font-family: -apple-system, система
- quote-text: 1rem (1.125rem на desktop)
- quote-author: 0.875rem
- line-height: 1.625 для читаемости
```

## ➕ Как добавить свои цитаты

1. Откройте файл `quotes.json`
2. Добавьте новый объект в массив:

```json
{
  "text": "Ваша цитата здесь",
  "author": "Имя автора"
}
```

3. Сохраните файл
4. Обновите страницу

### Пример:

```json
[
  { "text": "Существующая цитата", "author": "Автор" },
  { "text": "Новая цитата", "author": "Новый автор" }
]
```

## 🔄 Альтернативные источники данных

Вы можете заменить `quotes.json` на внешний API:

### Популярные API цитат:

```javascript
// Quotable API
const API_URL = 'https://api.quotable.io/random';

// ZenQuotes API
const API_URL = 'https://zenquotes.io/api/random';

// They Said So API
const API_URL = 'https://quotes.rest/qod';
```

**Примечание:** При использовании внешних API может потребоваться изменить логику обработки ответа в `main.js`

## 🧩 Возможные улучшения

- [ ] Избранные цитаты (сохранение в LocalStorage)
- [ ] Категории цитат (мотивация, мудрость, юмор)
- [ ] Поиск по автору
- [ ] Кнопка "Поделиться" в соцсетях
- [ ] Копирование цитаты в буфер обмена
- [ ] Анимация смены цитат
- [ ] Фоновые изображения
- [ ] Тёмная тема
- [ ] История просмотренных цитат
- [ ] Экспорт цитат в PDF/изображение

## 🐛 Обработка ошибок

Приложение корректно обрабатывает:

- ❌ Ошибки загрузки файла
- ❌ Проблемы с парсингом JSON
- ❌ Отсутствие интернета (для внешних API)
- ❌ Пустой массив цитат

При возникновении ошибки отображается сообщение:

```
"Error loading quote. Please try again."
```

## 📊 Статистика проекта

```
Строк кода (HTML): ~35
Строк кода (CSS): ~80
Строк кода (JS): ~45
Цитат в базе: 26
Авторов: 18
```

## 📜 Лицензия

Проект свободен для изучения, модификации и использования.

## Figma Project
https://www.figma.com/board/jqhq4k8drDesZSgl2326MA/quote_machine_styled?node-id=0-1&t=bSfXRhCeh7PSZDgX-1

## 📧 Контакты

Если у вас возникнут вопросы или предложения по улучшению проекта, вы можете связаться со мной через:

**Email:** zhanbolot46@gmail.com

**GitHub:** Yotagy-code
